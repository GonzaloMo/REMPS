name: REMPS_Virtual_Env
channels:
  - defaults
dependencies:
  - python=3.8.10
  - pip
  - pip:
    - -r requirements.txt
    # Environmnents
    - -e ./Environments/SimpleSatellite
    # Agents
    - -e ./Agents/PDDL-Planner
    - -e ./Agents/RL-Agent  