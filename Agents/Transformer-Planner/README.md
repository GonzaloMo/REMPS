# Transformer planner
This is a planner that uses a transformer network to create plans from a Domain and Problem PDDL files. It uses the GPT-NEO pretrained network and fine tunes it on the PDDL files. The network is then used to generate plans. 

##